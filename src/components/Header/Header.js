import NavBar from "./NavBar";
import "./Header.css";

const Header = () => {
    return (
        <header>
            <h1>GOOD TENNIS Shop</h1>
            <NavBar/>
        </header>
    );
}

export default Header;
